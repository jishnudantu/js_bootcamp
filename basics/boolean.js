// === equality operator
// !== not equal operator
// < less than
// > greater than
// <= less than equal to
// >= greater than and equal to
let temp = 90
let isFreezing = temp <= 31
// {} is a code block
if (isFreezing) {
    console.log('It is freezing outside!')
}
if (temp >= 100) {
    console.log('It is super hot!')
}
if (temp >= 45 && temp < 110){
    console.log('temperature is optimal for going out safely')
}
else{
    console.log('its cold outside!')
}

// && logical AND
// || logical OR
// ! NOT